This file lists the Python packages required by the application.

Install them all using: pip install -r requirements.txt

flask
joblib
numpy
pandas

We need scikit-learn for loading the model and scaler

scikit-learn

Gunicorn is the professional web server for Render

gunicorn